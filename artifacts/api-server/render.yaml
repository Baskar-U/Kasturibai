services:
  - type: web
    name: kasthuribai-api
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: CORS_ORIGIN
        value: https://kasthuribai.netlify.app
      - key: LOG_LEVEL
        value: info
    healthCheckPath: /api/health
    autoDeploy: true
